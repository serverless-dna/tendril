{
  "name": "tendril-agent",
  "version": "0.1.3",
  "description": "Tendril agentic sandbox — ACP sidecar powered by Strands SDK",
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=dist/main.cjs --format=cjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@strands-agents/sdk": "^1.0.0-rc.3",
    "@anthropic-ai/sdk": "^0.71.2",
    "openai": "^6.7.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "postject": "^1.0.0-alpha.6",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
